<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cc_num %>
    <%= form.number_field :cc_num %>
  </div>

  <div class="field">
    <%= form.label :cash_amt %>
    <%= form.text_field :cash_amt %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="field">
    <%= form.label :tip %>
    <%= form.text_field :tip %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

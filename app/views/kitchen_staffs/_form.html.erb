<%= form_with(model: kitchen_staff, local: true) do |form| %>
  <% if kitchen_staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kitchen_staff.errors.count, "error") %> prohibited this kitchen_staff from being saved:</h2>

      <ul>
      <% kitchen_staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :chef_name %>
    <%= form.text_field :chef_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
